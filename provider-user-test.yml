server:
  port: 8001
  error:
    path: /exception/error
eureka:
  client:
    service-url:
      defaultZone : http://localhost:7001/eureka/
  instance:
    hostname: discovery-server
    prefer-ip-address: true
spring: 
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/idea_database?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: xyjwsj
    initialSize: 20
    minIdle: 20
    maxActive: 64
    maxWait: 600
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: false
    maxPoolPreparedStatementPerConnectionSize: -1
mybatis:
  type-aliases-package: com.hoolai.access.foreign.provider.user.dao.mybatis.vo
  mapper-locations: classpath:com/hoolai/access/foreign/provider/user/dao/mybatis/mapper/*Mapper.xml
